export interface TagOrCategory {
  id: number;
  name: string;
  count: number;
  link: string;
}

export interface Settings {
  title: string;
  description: string;
  url: string;
  logo: string;
}
